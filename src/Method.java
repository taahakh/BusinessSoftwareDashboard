/*
* Constants that determine which method to use
* Used to stop repeating the creation of a new Button method everytime with all the extra baggage that comes with it
* */
public enum Method {
    ADD, REMOVE, UPDATE
}
